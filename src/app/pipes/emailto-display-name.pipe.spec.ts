import { EmailtoDisplayNamePipe } from './emailto-display-name.pipe';

describe('EmailtoDisplayNamePipe', () => {
  it('create an instance', () => {
    const pipe = new EmailtoDisplayNamePipe();
    expect(pipe).toBeTruthy();
  });
});
